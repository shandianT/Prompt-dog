# 复用指南：这套东西以后怎么用 v1.0（2026-09-08）

> 这份写给**下一次**——下次你要做一个新产品、要给一条现有流程上 AI、要给团队交一份设计文档加原型的时候，不用从零想「该怎么开始」。
>
> 分四段：① 包里有什么、② 三种最快的用法、③ 今天这场是怎么走的（阶段与产物）、④ 六条可复用的做法与换项目怎么套。最后是踩过的坑与文件地图。

---

## 一 · 包里有什么：三层资产

| 层 | 是什么 | 现在能用吗 | 位置 |
|---|---|---|---|
| **方法层** | PromptDog 技能本体：五个入口、六信号判级、预填交付、内嵌闭环、无人上岗、复盘回流 | **今天就能用**，不需要任何代码 | `SKILL.md` · `references/` · `assets/` |
| **资产层** | 三只已建好的工作狗（合同审查 / 标书撰写 / 内容营销），格式符合 Agent Skills 开放标准 | **今天就能用**，拖进工作区即上岗 | `kennel/` |
| **设计层** | 工作台的产品定义、SPEC、故事清单、46 张屏的原型与生成器、组件清单、开源选型 | **是图与施工图，不是产品代码** | `design/workbench/` |

一句话分清：**方法可用，界面是图。** 方法今天放进 Claude Code / Codex 就能跑；界面画完了但还没建。

---

## 二 · 三种最快的用法

### 用法 1（5 分钟）：把方法拿去用

```
把 Prompt-dog/ 整个文件夹放进你项目根目录
在根目录 AGENTS.md 加一段路由（原文见 QUICKSTART.md 第二节）
```

然后直接说话，五种说法各走一条线：

| 你说 | 它给你 | 走哪条线 |
|---|---|---|
| 「帮我写一个把会议纪要总结成三条要点的提示词」 | 提示词全文 + 验收标准 + H 假设 | 新建 · L1 |
| 「这段提示词帮我看看」（贴一段） | 十维打分 + 三条建议 + 改进版 + 回归对照 | 诊断 |
| 「我们投标流程是……哪些能交给 AI」 | 三泳道流程图 + 卡点/缺口/待打通 + 打包建议 | 流程重构 |
| 「做一个小餐馆排班工具」 | 五问首行 + 一页产品定义 + 故事清单 | 设计 |
| 「每周一自动出竞品周报」 | 资产包（环节提示词 + 验收清单.json + 上岗.sh） | 新建 · L3 |

**怎么算成功**：它不反问，直接给完整产物。它连续反问 → 回「按技能流程来，出选择题」；它每步等确认 → 回「直接做，按推荐」。

### 用法 2（半天）：给你自己的产品做一遍设计

照搬 `design/workbench/` 的做法，换成你的产品。第四节「换个项目怎么套」有分步话术。产出是：一页产品定义 + 故事清单 + 一套可点的高保真屏 + 组件清单。

### 用法 3（一周起）：把工作台建出来

按 `design/workbench/stories.json` 的顺序，一次一条。S01 是工程骨架（Vite + React + TS + `@xyflow/react` v12），选型理由在 `开源选型.md`，不用重新论证。**注意：18 条全部 `passes: false`，一条都没开工。**

---

## 三 · 今天这场是怎么走的

一天走了十一个阶段。列出来不是为了复述，是因为**顺序本身可以复用**——每个阶段的产物是下一个阶段的输入。

| # | 起因（我说的话） | 做了什么 | 产物落在哪 |
|---|---|---|---|
| 1 | 规则有问题：重复确认、提前停止、过度工程 | 审计全部指令文件，改自主性 / 澄清 / 批准 / 任务完成四类规则 | `SKILL.md` · `references/` · `assets/` |
| 2 | 「做个工作台原型」 | 用 design 技能画 6 屏 + 组件表 | 设计画布 v1 |
| 3 | 「流程重构那屏怎么画」 | 双泳道 + 四类来源标签 + 插槽装配 | 02b 屏（后来并入 07） |
| 4 | 「以产品经理角度评估」 | 找出真问题：画布才是中心，不是聊天窗口 | 八条设计原则 + 07 可交互画布 |
| 5 | 「今天的内容怎么沉淀」 | 分类沉淀：方法进 references，资产进 kennel，经验进复盘 | `复盘/` 两级制 |
| 6 | 「怎么当 skill 复用起来」 | 借鉴 ralph 的循环，写成无人上岗 | `references/run-loop.md` · `验收清单.json` · `上岗.sh` |
| 7 | 「怎么保证以后输出高质量设计文档」 | 加「设计」入口 + 四视角找茬 + 12 项门槛 | `references/design-mode.md` |
| 8 | 「我不懂怎么用」 | 写从 0 到 1 的使用逻辑：第一周五步 | `使用逻辑.md` · 00 引导屏 |
| 9 | 「已有功能都可以用对应的开源项目」 | 逐项定选型，查许可证 | `开源选型.md` |
| 10 | 「把每一步都设计出来」 | 逐步交互规格：八个字段 × 十二步 | `交互流程.md` · 12 旅程屏 |
| 11 | 「每一步都画出来 + 组件化 + 评审」 | 先评审后画：40 步三栏评审 → 新画 20 屏 → 组件化两层 | `逐步评审.md` · `组件清单.md` · 画布 v3 |

**顺序里的规律**：规则 → 原型 → 评估 → 沉淀 → 补方法 → 补使用逻辑 → 定选型 → 补细节 → 评审重画。前四步是发散，后面每一步都在收敛。**每次我问「这个怎么用 / 这里合理吗」，都会逼出一份新文档**——不是文档写多了，是问题问对了。

---

## 四 · 六条可复用的做法

### 1. 先评审后画，不要边画边想

今天最有效的一步：动手画之前，先把每个功能的每一步列成表，每步三栏——**合理**（为什么保留）、**缺少**（要加什么）、**没用**（删什么），再加一栏**决定**。评审完再照决定画。

格式直接抄 `逐步评审.md`：

```markdown
| 步 | 屏 | 用户做 | 系统做 | 合理 | 缺少 | 没用 | 决定 |
```

判断依据只有三条（换成你的产品要换，但必须只有三条左右，多了就不是标准是感觉）：
- 这一步能不能不问就先做出来
- 用户在这一步只需要说编号或点一下
- 停下来的地方是不是不可逆动作前

### 2. 缺的多数是「状态」，不是「屏」

今天补的 20 屏里 **16 屏是状态**：生成中、生效后、运行中、完成、止损、空态。原来 15 块画板覆盖了全部功能，但每个功能只画了一个瞬间——而用户恰恰在这些状态里等待和判断。

**下次的检查清单**：每一屏问一遍——它的空态、进行中、成功、失败、被打断，画了几个？

### 3. 源码是唯一真相，一份代码两种输出

屏幕不是手画的，是 Python 函数生成的 `.dc.html`。好处不是省事，是**不漂移**：

- 组件表里的每一块，都由生成屏幕的**同一个函数**产出——所以组件表永远等于屏幕的真实样子
- 一个 `IMPORTS` 开关决定输出 `<dc-import>` 版（给画布，真组件可传参）还是内联版（给无头浏览器截图）——两份不会漂移，因为是同一段代码

```bash
python3 flow.py                            # 画布版
python3 flow.py --inline --out /tmp/inline  # 截图版
```

### 4. 组件化落两层

| 层 | 做法 | 为什么 |
|---|---|---|
| 真组件 | 可传参、被各屏引用（`<dc-import>`），改一处所有屏跟着变 | 框架件（侧栏 / 顶栏 / 输入栏 / 节点）改动最频繁 |
| 状态表 | 每个组件一张画板列出全部状态 | 内容件（卡片 / 行 / 标签）的价值在「有几种状态」，不在「能不能传参」 |

再写一份 `组件清单.md`：每个组件的 **props · 状态 · 用在哪些屏 · 用什么开源件承接 · 设计源函数**。开发照表建，不用猜。

### 5. 状态机对账：最便宜的整体检查

画完所有屏，列一张表：**从哪屏 → 什么动作 → 到哪屏 → 停不停**。然后检查三件事：

1. 每屏至少一个出口（没有出口的屏就是断链）
2. 停机点没有变多（今天全程只有三个：按这个建 / 人工确认点 / 改版不改版）
3. 一个数字只有一个来源（`验收清单.json` → 进度；flow JSON → 计数；版本号 → 一处）

这一步今天发现了两个规格与原型的脱节，都是画静态图时才暴露的。

### 6. 材料先行 + ★ 预填：不问问题也能开始

贯穿整套方法的核心：**先做出来再纠错**。有材料就从材料反推，没材料按行业惯例出草稿并标 H 假设。卡片是纠错窗口，不是审批关卡——不回复也走完。

只在**不可逆动作前**停（发送 / 提交 / 付款 / 发布）。

---

## 五 · 换个项目怎么套

### 场景 A：一个新产品想法 → 设计文档 + 原型

```
第 1 步｜说想法（不用写文档）
  「做一个 <产品>，用户是 <谁>，现在他们 <痛点>」
  → 出：五问首行 + 一页产品定义 + 故事清单（S01…）
  → 看：五问 30 秒答不出哪一问，就是定义没写清

第 2 步｜逐步评审（先别画）
  「按 逐步评审.md 的格式，把每个功能每一步列出来，
   每步给合理 / 缺少 / 没用 / 决定」
  → 出：一张评审表 + 缺少汇总 + 没用汇总

第 3 步｜按决定画
  /design 按评审表的决定画出每一步，每行一个功能，
        行首便签写这一行的评审
  → 出：画布，每行一个功能

第 4 步｜组件化
  「把框架件抽成可传参的真组件，其余每个组件一张状态表，
   再写 组件清单.md 映射到 shadcn/ui」
  → 出：组件页 + 施工图

第 5 步｜对账
  「做一次状态机对账：每屏的出口、停机点几个、一个数字几个来源」
  → 出：断链清单（通常有 1–3 处）
```

**耗时参考**：今天第 2–5 步（评审 40 步、画 20 屏、组件化、对账、发布）大约一个工作日的对话量。

### 场景 B：一条现有流程要上 AI

```
第 1 步｜说流程 + 贴材料（材料决定八成质量）
  「我们 <什么> 流程：<谁做什么> → <谁做什么>，卡在 <哪>」
  顺手把流程文档、群聊记录、表格拖进来
  → 出：三泳道图 + 卡点/缺口/待打通 + 五问数字 + 打包建议

第 2 步｜三十秒读图
  找三个没看过的人，三十秒内答出：哪些自动、人在哪、卡在哪、
  缺什么、要打通什么。答不出就是图没画对，回第 1 步纠错

第 3 步｜先建一只（楔子）
  「先建 <打包建议里的第一只>」
  → 出：资产包（环节提示词 + 验收清单.json + 上岗.sh）

第 4 步｜用真实材料跑一次
  → 只在对外发送前停

第 5 步｜复盘 → 回到图上
  「复盘」→ 四指标 + 回归 + 规律
  切「对照现状」看卡点从 N 变成 0，决定下一只建谁
```

**别做的事**：不要一次建多只狗；不要先整理材料再开始；不要等图完美再动手。

### 场景 C：只是想改好一条提示词

贴进去就行，零提问。出十维打分 + 三条建议（每条带原文与机制）+ 改进版全文 + 回归对照。**改进必须用真实输入各跑一次验证**，不能只看分数。

### 场景 D：给团队交一份能开工的东西

三件套 + 图，交付顺序固定：五问首行 → 一页产品定义 → `stories.json` → 图 → H 假设 → 可选纠错卡。交付前过四视角找茬（老板 / 搭建者 / 执行者 / 反方，各至少一条反驳）与 12 项门槛（`references/design-mode.md`）。

---

## 六 · 踩过的坑（省下的时间）

| 坑 | 现象 | 解法 |
|---|---|---|
| f-string 里放反斜杠 | `SyntaxError: f-string expression part cannot include a backslash` | 把含 `\` 的部分预先算成常量再插值 |
| 画布发布被拒 | publish 报 stale / 未读过 | 先 `Artifact action:"read"` 拉回线上版本，确认没人改过再发 |
| 画板高度估小了 | 内容被裁掉，且**没有任何报错** | 按垂直节奏累加高度 + 5% 余量；宁大勿小（多出来的部分只是背景） |
| 只在编辑器里看 | 慢，且一次只能看一屏 | 用内联版 + 无头 Chromium 单屏截图，一次几十张，看完再进编辑器抽查 |
| 生成的 SVG 自闭合标签 | 模板解析器不认 | 生成时把 `<path ... />` 展开成 `<path ...></path>`（`gen.build` 里的 `canon`） |
| 顶栏右侧塞自定义 HTML | 每屏各写一遍，改一次要改十处 | 收敛成 `actions=[(label, kind, icon)]`，四种 kind 够用 |
| 按钮文字换行 | 中文按钮在窄栏里竖着排 | 缩短文案 + `white-space:nowrap` |
| 评审意见与原型脱节 | 规则写了但原型没实现（如「受保护」节点） | 画静态状态时会暴露；发现了就同步改原型 |

---

## 七 · 文件地图：想干什么读哪份

| 想干什么 | 读这份 |
|---|---|
| 先跑起来 | `QUICKSTART.md` → 第八节「第一周怎么用」 |
| 搞懂方法全貌 | `SKILL.md`（主循环 ①–⑨） |
| 看某个入口怎么走 | `references/`：`process-rebuild.md`（流程重构）· `design-mode.md`（设计）· `diagnose.md`（诊断）· `run-loop.md`（上岗与复盘） |
| 定方向 | `design/workbench/产品定义.md` |
| 写代码前定技术栈 | `design/workbench/开源选型.md` |
| 实现每一步交互 | `design/workbench/交互流程.md` |
| 知道每一步为什么这样画 | `design/workbench/逐步评审.md` |
| 建前端组件库 | `design/workbench/组件清单.md` |
| 按顺序开工 | `design/workbench/stories.json` |
| 看设计稿 | `截图/` 或在线画布（链接见 `design/workbench/README.md`） |
| 重新生成画板 | `design/workbench/README.md`「重新生成画板」 |
| 回顾今天怎么走的 | `复盘/2026-09-08-工作台设计与规则沉淀.md` |

---

## 八 · 下次开场可以直接说的话

复制粘贴即可：

```
读一遍 复用指南.md 和 SKILL.md，然后按「场景 A / B / C / D」里的
<某一个> 帮我走一遍，我的项目是：<一句话>
```

```
按 逐步评审.md 的格式，把 <我的产品> 的每个功能每一步列出来评审，
每步给合理 / 缺少 / 没用 / 决定，评审完再画
```

```
把这套设计按 组件清单.md 的格式抽成组件，映射到 shadcn/ui，
框架件做成可传参的真组件
```
