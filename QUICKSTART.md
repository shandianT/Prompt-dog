# 快速上手手册（小白版）

跟着做就行，全程不需要懂提示词工程。

## 一、你手里有什么

一个 `skill-architect` 文件夹 = 一个「教 AI 帮你写提示词」的技能包。装好后，你的 AI 会变成提示词架构师：你说一句模糊需求，它出选择题让你确认，然后交付能直接用的提示词或工作流。

## 二、第一次安装（5 分钟，只做一次）

**用 Codex / Claude Code 这类编程 Agent（推荐）：**

1. 打开你平时让 Agent 干活的项目文件夹
2. 把 `skill-architect` 文件夹整个放进去（根目录）
3. 打开根目录的 `AGENTS.md`（没有就新建一个），粘贴进去：

```markdown
## 提示词架构师

当用户要求：写/优化提示词、诊断现有提示词、设计 SOP 或 AI 工作流时——
读取 prompt-dog/SKILL.md 并严格按其流程执行（它会指引你按需读取
references/ 下的对应文件）。不要凭默认习惯直接回答这类请求。
```

4. 保存，完成。

**其他情况**：只有一个指令框、放不了文件的 AI → 打开 `assets/system-prompt.md`，从分隔线以下全文复制，贴进指令框（或对话第一条消息）。

## 三、验证装没装好（1 分钟）

对 AI 说下面三句话，对照反应：

| 你说 | 装好了的反应 |
|---|---|
| 帮我写一个把会议纪要总结成三条要点的提示词 | 不反问，直接给完整提示词 |
| 我是做跨境电商的，想让 AI 帮我做客服质检 | 出一张选择题卡片，选项带 ★ 推荐 |
| 帮我看看这个提示词：（随便贴一段） | 先给一张十项评分表 |

三条都对 = 成功。

## 四、日常怎么用（只有三种说法）

| 你想要 | 你就说 |
|---|---|
| 一个新提示词 | 帮我写一个……的提示词 |
| 改进手头的提示词 | 帮我看看这个提示词：（粘贴原文） |
| 自动化流程/复杂任务 | 帮我设计一个……的工作流 |

## 五、它出选择题时，怎么回最省事

- 全信它的推荐 → 回 **「都按推荐」**
- 只有一两处不同 → 回 **「1B 3C 其他默认」**（题号+字母）
- 选项全不对 → 直接一句话说你的实际情况，它会重出
- 看不懂某题 → 问「第 2 题什么意思」

回完它会复述一行「生效：…」，然后直接干活，不会再来回确认。

## 六、拿到结果后怎么用

- **一段提示词**：复制，把 `<尖括号>` 换成你的实际内容，发给 AI 就能用
- **带步骤的 SOP / 工作流**：当说明书用——照顺序把每个环节的提示词依次贴给 AI；在 Codex 里可以直接说「按这个工作流执行」
- **开头的 H1/H2 假设清单**：哪条不符合你的情况，就说「H2 不对，实际是……」，它只改那一处

## 七、遇到问题说这几句

| 情况 | 你说 |
|---|---|
| 它连续问开放式问题 | 「按技能流程来，出选择题」 |
| 想改交付里的某处 | 说编号：「H2 不对」「环节 3 改成……」 |
| 觉得方案太复杂 | 「简化，按低一级做」 |
| 想知道它为什么这么设计 | 「展开判级」「为什么用提示链」 |

就这些。从第四节的三句话开始用起来即可。
