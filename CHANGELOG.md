# 更新记录

> 这里记录 PromptDog 的主要产品迭代，让访问者能看到项目在持续运营和更新。

## 2026-09-20

### S03 画布、泳道与五问数字
- **画布第一次长得像产品**：`workbench/src/canvas/`，React Flow 承载。`FlowNodeCard` 包成自定义节点（端口换 `<Handle>`，复用 `.fnode__port` 的样子）；三条泳道画在 `ViewportPortal` 里，与节点同一套坐标、随平移缩放一起动；缩放限 50%–160%；顶栏五个数字直接算自 `fiveNumbers()`。页面 `#/canvas` 渲染投标示例：19 节点、23 线、自动 7 · 人 7 · 卡点 2 · 缺口 1 · 待打通 4。
- **`check:ui` 加画布断言**：五个数字、节点 / 线 / 泳道 / 端口计数、滚轮放大 scale 变大、封顶 160%、到底 50%、缩到 50% 后按**屏幕坐标**点 n9 能进入选中态——坐标换算错了就点不中，这条替代了「肉眼看缩放对不对」。
- **刻意没开的**：拖节点（S05 加移动与撤销、S09 加泳道规则——没有规则的拖动会让数据节点跑出数据泳道，违反 x-compat 规则 2）；五种连线（S04，现为素线）；侧栏与属性面板（S06 / S13+）。
- `workbench/README.md` 顶部补了「这个工程是干什么的」：方法层已完成今天就能用，产品层是为了让不打开 Claude Code 的人也能用；前三条故事为什么看着不像产品。

### S02 FlowNode 组件与变体页
- **`FlowNode` 组件**（`workbench/src/components/FlowNode.tsx` + `.css`）：kind 5 × role 3 × flag 4 全部变体，选中 / 连线中 / 运行中 / 压暗四种交互态，对照 chip 三种，子图、受保护、运行态三种角标，端口颜色按产物类型。纯展示件——位置与拖拽由画布负责，S03 包成 React Flow 自定义 node 时端口换 `<Handle>`，传 `ports="none"` 即可。
- **变体页** `#/flow-node`：60 个组合逐格渲染的真组件，不是截图；另有交互态、对照 chip、角标、可点的角色徽章与端口配色表。不引 Storybook，变体页就是它的替代品。
- **`npm run check:ui`**：真浏览器验收，不靠肉眼看截图。自己起 preview，用 CDP 读计算样式逐条断言 SPEC §6.3——矩阵格数、人节点琥珀 1.5px 实线、数据节点暖灰底且不画角色徽章、卡点红实边 / 缺口琥珀虚边 / 待打通蓝虚边、三种标记压右上角、对照 chip 压左上角、「新增」chip 绿底、控制台无报错。**颜色不比十六进制，比 `tokens.css` 里同一个变量的解析结果**，所以「组件里偷偷写了个相近的颜色」也会被抓到。
- **两件值得记下的**：① 断言跑在 devicePixelRatio = 2——DPR 1 时 Chrome 把 1.5px 边框的「用值」舍成 1px，在那个密度下断言 1.5px 会假报错，规范要的是 1.5px 就得在看得见它的密度下验；② 端口配色以 schema 的 `x-compat.port` 为准（六种产物类型），原型 CSS 只定义了四种（`file` / `event` 漏了），补齐并加了 key 漂移检查。
- 标签集中到 `src/flow/labels.ts`，页面与组件不再各写一份中文说法。

## 2026-09-19

### 工作台前端开建：S01 工程骨架与数据契约
- **`workbench/`**：React 19 + TypeScript + Vite 7 + Tailwind v4；画布底座 `@xyflow/react` v12，导出 `html-to-image`（锁 1.11.11）+ `jspdf`（≥4.2.1，更低版本有 critical 漏洞），文本档图 `mermaid`，运行时校验 `ajv`。选型理由不重复论证，链到 `design/workbench/开源选型.md`。
- **数据契约接入运行时**：`design/workbench/flow.schema.json` 是唯一来源，工程直接 import 不拷贝副本。四道检查，任一不过 `npm run check` 退出码 1——schema 校验、`types.ts` 与 schema 的枚举漂移、连线引用完整性、`x-compat` 四条规则；`scripts/check-guards.ts` 用十个故意改坏的流程图证明每道检查真的会报错。
- **示例数据单一来源**：`design/workbench/export_flow_json.py` 从 `flow.py` 的 `NODES` / `EDGES` 导出 `workbench/sample/投标流程.json`，原型与工作台共用一份（19 节点含子图 7 · 23 条线 · 自动 7 · 人 7 · 卡点 2 · 缺口 1 · 待打通 4，与 stories.json S03 / S04 的验收数字一致）。
- **校验查出两处契约漂移并修掉**：`children` 在 `flow.py` 里是 `{nodes, edges}` 而 schema 要 `children[]` + `childEdges[]`（导出时归一）；`irreversible` 字段原型在用但 schema 没有（`additionalProperties: false` 会拒收），而 `x-compat` 规则 3「不可逆动作前的人定节点不可改为 auto」正需要它——补进 schema，规则才能机器检查。
- **SPEC §7 的 token 落地**：`src/styles/tokens.css` 用 Tailwind v4 `@theme` 定义颜色 / 字号 / 圆角 / 控件高度，组件不再写死颜色。首屏是契约自检页（不是产品的 01 首页）。

## 2026-09-08

### 流程重构入口、无人上岗与工作台设计沉淀
- **复用指南**：`复用指南.md` 把今天这套做法整理成可搬走的东西——三层资产各自的状态（方法可用 / 工作狗可用 / 界面是图）、三种最快的用法、十一个阶段的走法、六条可复用的做法（先评审后画 · 缺的多数是状态不是屏 · 源码是唯一真相一份代码两种输出 · 组件化两层 · 状态机对账 · 材料先行加 ★ 预填）、四个场景的分步话术（新产品 0→1 / 现有流程上 AI / 改一条提示词 / 交设计文档）、八条踩过的坑与文件地图。
- **每个功能每一步**：用设计画布把每个功能的每一步都画出来并逐步评审——`design/workbench/逐步评审.md` 对入口 / 诊断 / 新建 / 流程重构 / 设计 / 上岗 / 复盘与资产 每一步给出「合理 / 缺少 / 没用 / 决定」，并做状态机对账（每屏有出口、停机点仍是三个、一个数字只有一个来源）。据此新画 20 屏（01.2 / 01.3 入口状态、13.1–13.3 诊断线、02.2 生效复述、03.2 单提示词交付、07.0 / 07.2–07.5 画布五个静态状态、11.2 故事清单、11.3 四视角找茬、05.1 / 05.2 / 05.4 / 05.5 上岗四态、10.2 达标发布、14 组件库），删改 7 处（01 重复卡、03 四道保险换真实计数、05.3 promise 标记、07「试跑」改「运行态」并读 验收清单.json、08 主按钮改成老板的决定、02.1 加换形态逃生口、02b 移出画布）。画布分两页：「每个功能每一步」按功能排成行、行首便签是评审；「组件」页——侧栏 / 顶栏 / 输入栏 / 节点做成可传参的真组件（`<dc-import>` 引用，改一处所有屏跟着变），其余 12 个组件每个一张状态表，开发映射（props · 状态 · 开源件）在 `组件清单.md`。原型同步修正：不可逆前的人定节点受保护、应用建议后数据节点不再跳出数据泳道。
- **设计模式**：新增入口——用户给一个产品 / 功能 / 界面想法或要 PRD、设计文档时不判级，按 `references/design-mode.md` 材料先行出三件套（五问首行 + 一页产品定义 + `stories.json` + 可选 SPEC 骨架），交付前过四视角对抗审查与可打钩门槛；骨架进 output-templates.md「设计模式产物」，必带验收项表加「设计文档 / PRD」「图」两行；evals 新增 11–13。
- **产品主线**：SKILL.md 主循环延到 ⑦ 上岗 → ⑧ 复盘 → ⑨ 回流；串联契约（循环内上一轮停机标记 = 下一轮动作）写在 run-loop.md，并联只在两处合法写在 complexity-routing.md；全图 `design/figures/product-process.svg`。
- **图档位由环境决定**：process-rebuild.md 加三档表（高保真 / 文本图 / 泳道表）与四个镜头，任何画图技能都不是必备依赖；SKILL.md 与 verification.md 环境封顶段同步。
- **回流表**：`复盘/README.md` 列出六类交付物各变成什么资产、落到哪个现有文件、触发条件（同一规律出现 2 次即候选回流），回流本身有三条门槛。
- **流程重构模式**：新增入口——用户描述一个部门 / 公司的现有流程（谁做什么、卡在哪）时不先判级，按 `references/process-rebuild.md` 材料先行画出目标态流程图（三条泳道 + 卡点 / 缺口 / 待打通 + 五种线），附五问数字、诊断清单与打包建议；打包建议里的每只工作狗再转主循环判级。单文件版（v1.3）同步。
- **无人上岗**（借鉴 snarktank/ralph）：`references/run-loop.md` 定义状态文件 `验收清单.json`（环节 × 验收项 × passes）、每轮新上下文只做一个环节的 `上岗.sh` 循环、三种停机标记（交付 / 止损 / 待确认）、复盘两级制（日志追加 + 「已知规律」抬升到 AGENTS.md）；不借用跳过权限与「先问完再做」。
- **环节尺寸规则**：每个环节一个上下文能做完，做不完按产物 / 动作 / 对象拆；环节顺序即产物依赖顺序（SKILL.md 构建产出、output-templates 资产包形态）。
- **按环节类型必带的验收项**（verification.md）：文档要有来源、结构化要过 schema、代码要过检查、界面要在浏览器里验、对外动作前要有人工确认点。
- **行业候选库**新增招投标 / 标书；复杂度边界案例新增「一段流程」；evals 新增用例 9（流程重构）、10（无人上岗资产包）。
- **犬舍 · 标书撰写 v1.2**：随包提供 `验收清单.json`、`上岗.sh`、`复盘/日志.md`，AGENTS.md 增加无人上岗路由与「已知规律」。
- **开源选型**：`design/workbench/开源选型.md` 逐项定下每个能力用哪个现成开源项目——画布依赖 `@xyflow/react`（MIT，Dify / Langflow / Flowise 的画布底座都是它，所以不 fork 任何一家）、导出用 `html-to-image@1.11.11` + `jsPDF`、文本档图用 `mermaid`、评测用 `promptfoo`、前端壳用 `shadcn/ui` + `Tremor`；观测不引入平台，只把字段命名对齐 OTel GenAI 语义约定。同时确认资产包格式**本来就符合 Agent Skills 开放标准**，README 口径随之修正；与 Dify 的关系定为上下游（目标态流程图单向导出 Dify DSL），不是竞品。产品定义的技术栈待拍板项就此结清。
- **从 0 到 1 的使用逻辑**：`design/workbench/使用逻辑.md`（第 0 天准备、第一周五步、节奏、入口选择、常见误区）、旅程图 `design/figures/first-run.svg`、画布新增 00 首次引导屏；QUICKSTART 加第八节「第一周怎么用」。
- **逐步交互规格**：`design/workbench/交互流程.md` 把「用户怎么用」拆到每一步——安装、说流程贴材料、读图、纠错、按这个建、确认卡、交付入犬舍、上岗、人工确认点、复盘、回到图上，每步写清在哪屏 / 你看到什么 / 你做什么 / 系统做什么 / 产物 / 停在哪 / 不回复怎样 / 出错怎么办；另有六条异常路径与停机总表（全流程只有三个地方会停，其中只有人工确认点在不回复时会一直停着）。画布新增 12 用户旅程屏。
- **工作台画布补全**：新增 08 汇报模式（与 07 同一份流程数据渲染的只读放大泳道图 + 五问数字 + 三张诊断结论卡）、09 组件详情（契约 · 参数 · 7 环节子图 · 复用流程与节点号 · 版本与改动）、10 复盘与回归（四指标回填 · 回归对照 · 发布门槛 · 回流建议）；SPEC §5 里已列规格但没画的三屏就此补齐，14 块画板全部到位。源在 `design/workbench/screens_v3.py`。
- **工作台画布 v2**：01–05 按产品目标重画（四个入口、预填交付的纠错卡、交付页的入犬舍 / 画布出口与未达标清单、犬舍状态来自 验收清单.json、上岗屏停在人工确认点），新增 11 设计屏；源在 `design/workbench/screens_v2.py` 与 `design_screen.py`。
- **工作台设计资产** `design/workbench/`：一页产品定义、构建规范 `SPEC.md`、按依赖排序的 `stories.json`、流程图契约 `flow.schema.json`、原型生成器与交互验证脚本。
- **仓库级复盘** `复盘/`：日志追加、规律抬升的两级制，首篇记录 2026-09-08。

## 2026-09-07

### 规则审计：自主性 / 澄清 / 批准 / 任务完成
针对「重复确认、提前停止、过度工程、任务做不完」四类问题审计全部指令文件，修正如下：

- **澄清**：L3/L4「首轮永不为零」与「不影响设计的题不许问」互相矛盾——改为按「架构级不确定项」数决定出不出卡：0 项零提问直接交付（任何级别）；L1/L2 仅 1 项时预填交付（按 ★ 交付 + 附可选纠错卡）；运行环境已知（含「就在当前 Agent 里跑」）时跳过环境题。
- **无人值守**：新增规则——用户说「直接做 / 不要问我」或无人交互时，不出卡、全取 ★、一次交付；未答项一律取 ★，删除与之冲突的「按最保守选项设计」。
- **验收卡**：原「量化 / 格式」成题与 SKILL「只影响措辞篇幅→假设」冲突——验收卡只保留错误代价（推断不准时）与场景化红线两类可成题项，其余写 H；L1 不再为错误代价单独占一轮；S3 用途不明统一按「中」（原两处分别写「取低分」与「不默认低」）。
- **人工确认点**：原选型表写「仅不可逆动作」、配比原则却对所有高风险任务强制插入——统一为只在不可逆动作前；验证强度受运行环境能力封顶，无搜索工具时交叉核查降级为 [待核实] + 人工核对清单。
- **止损线**：「最大 3 轮 + 连续 2 轮不增加」在数学上只能在第 3 轮末触发，等于没有止损——改为「一轮精调后打钩数不增加即止损」；每轮只重跑被精调环节及下游；止损后已达标部分 + 未达标清单 + 卡点报告同时交付，不再「降级 或 上报」二选一。
- **会话内试跑**：原「回到构建阶段」无上限——改为修 2 轮 + 重设计 1 次，仍不达标照常交付并列未达标清单；有工具真实执行，不能执行的环节静态走查。
- **诊断模式**：原必须等用户答完卡片才给改进版——改为同一回复内打分 + 改进版 + 回归对照，仅改法真有分歧时附可选纠错卡。
- **过度工程**：资产包（含复盘/回归目录）只在场景重复使用时建，一次性任务交付单文档。
- **用户覆盖**：用户明确说「简化 / 按低一级」或「就要多智能体」→ 照办记 H0，不重新辩论。
- **安装路径**：QUICKSTART 让用户放 `skill-architect` 文件夹、路由却指向 `prompt-dog/SKILL.md`，README 又写 `Prompt-dog/`——统一为 `Prompt-dog/`。
- **单文件版**：`system-prompt.md` 与 `persona-single-field.md` 由同一份正文生成（v1.2），避免再次分叉。
- **犬舍**：合同审查（立场缺省按委托方推断、落盘不需确认、退回上限、单人设版不再每阶段等「继续」）、标书撰写（应答矩阵不等过目、废标项存疑照常交付、补建 `招标文件/` 目录）、内容营销（选题候选按 ★ 直接写、素材快收改选择题）。
- **evals**：更新用例 3、4，新增无人值守、L3 零提问、L1 不问错误代价三个用例。

## 2026-07-24

### 本次更新
- 同步 `prompt-dog 6` 版本内容到 GitHub。
- 更新官网首页、英文页面、README、截图和 Vercel 静态站点文件。
- 保留并更新犬舍资产包：标书撰写 Agent、合同审查 Agent、内容营销 Agent。
- 清理页面中不必要的作者标注和复杂方法论口号，让展示更简洁。

### 展示与部署
- 维护 Vercel 在线演示站点：<https://prompt-dog.vercel.app/>
- 首页继续作为 Vercel 默认入口，访问站点即可看到演示页面。
- README 保留在线演示入口和犬舍入口。

## 2026-07-23

### 开源发布
- 初始化 PromptDog 开源仓库。
- 添加 MIT License。
- 发布 PromptDog 核心 Skill、references 方法库、assets 单人设版本和 evals 测试用例。

### 首批工作狗
- 新增合同审查 Agent 资产包，包含启动指令、审查流程、分环节 prompts、输出模板、复盘目录和回归测试目录。
- 形成“PromptDog 造 Agent，工作狗上岗”的产品结构。

### 展示材料
- 上传 HTML demo 页面，用于更直观展示 PromptDog 的交互和价值。
- README 增加在线演示入口。
